# Branch deletion is managed by the repository's "Automatically delete head branches" setting.
name: Branch housekeeping
on: workflow_dispatch
permissions:
  contents: read
jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Enable automatic head-branch deletion in repository settings.'
