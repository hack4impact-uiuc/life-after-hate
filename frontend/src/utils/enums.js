const roleEnum = {
  ADMIN: "ADMIN",
  VOLUNTEER: "VOLUNTEER",
  PENDING: "PENDING"
};

const modalEnum = {
  RESOURCE: "RESOURCE",
  USER: "USER"
};

module.exports.roleEnum = roleEnum;
module.exports.modalEnum = modalEnum;
