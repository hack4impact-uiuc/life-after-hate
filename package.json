{
  "name": "life-after-hate",
  "version": "2.0.0",
  "private": true,
  "description": "Development commands for the Life After Hate resource directory",
  "license": "MIT",
  "engines": {
    "node": ">=24 <27"
  },
  "scripts": {
    "test": "npm run test:server:unit && npm run test:turso && npm --prefix backend test && npm --prefix frontend test && npm --prefix scripts/db_backup/backup test",
    "build": "npm --prefix frontend run build",
    "test:e2e": "node turso/build.mjs && npm --prefix frontend run test:e2e",
    "lint": "node scripts/check-syntax.cjs",
    "format": "prettier --write backend frontend/src scripts/db_backup/backup '*.md'",
    "build:vercel": "node turso/build.mjs && node turso/build-frontend.mjs",
    "test:turso": "node turso/build.mjs && node --test server/test/*.test.mjs turso/*.test.mjs",
    "check:server": "tsc -p server/tsconfig.json",
    "dev:demo": "node turso/build.mjs && node server/scripts/preview.mjs",
    "test:server:unit": "node frontend/node_modules/vitest/vitest.mjs run --root server",
    "test:coverage": "npm run test:server:unit -- --coverage && npm --prefix frontend run test:coverage"
  },
  "devDependencies": {
    "@types/node": "^26.5.1",
    "esbuild": "^0.28.2",
    "prettier": "^3.8.0",
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "@hono/node-server": "^2.1.1",
    "@libsql/client": "^0.18.0",
    "fuse.js": "^7.5.0",
    "hono": "^4.13.7",
    "jose": "^6.2.12",
    "zod": "^4.6.5"
  }
}
